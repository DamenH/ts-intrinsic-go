//// [tests/cases/compiler/intrinsic/customIntrinsics.ts] ////

=== customIntrinsics.ts ===
// Core tests for Intrinsic<typeof fn, [Args]>.

const capitalize = (s: string) => s.charAt(0).toUpperCase() + s.slice(1);
>capitalize : (s: string) => string
>(s: string) => s.charAt(0).toUpperCase() + s.slice(1) : (s: string) => string
>s : string
>s.charAt(0).toUpperCase() + s.slice(1) : string
>s.charAt(0).toUpperCase() : string
>s.charAt(0).toUpperCase : () => string
>s.charAt(0) : string
>s.charAt : (pos: number) => string
>s : string
>charAt : (pos: number) => string
>0 : 0
>toUpperCase : () => string
>s.slice(1) : string
>s.slice : (start?: number, end?: number) => string
>s : string
>slice : (start?: number, end?: number) => string
>1 : 1

function range(startOrEnd: number, end?: number): number[] {
>range : (startOrEnd: number, end?: number) => number[]
>startOrEnd : number
>end : number | undefined

    const s = end === undefined ? 0 : startOrEnd;
>s : number
>end === undefined ? 0 : startOrEnd : number
>end === undefined : boolean
>end : number | undefined
>undefined : undefined
>0 : 0
>startOrEnd : number

    const e = end === undefined ? startOrEnd : end;
>e : number
>end === undefined ? startOrEnd : end : number
>end === undefined : boolean
>end : number | undefined
>undefined : undefined
>startOrEnd : number
>end : number

    return Array.from({ length: Math.max(e - s, 0) }, (_, i) => s + i);
>Array.from({ length: Math.max(e - s, 0) }, (_, i) => s + i) : number[]
>Array.from : { <T>(arrayLike: ArrayLike<T>): T[]; <T, U>(arrayLike: ArrayLike<T>, mapfn: (v: T, k: number) => U, thisArg?: any): U[]; <T>(iterable: Iterable<T> | ArrayLike<T>): T[]; <T, U>(iterable: Iterable<T> | ArrayLike<T>, mapfn: (v: T, k: number) => U, thisArg?: any): U[]; }
>Array : ArrayConstructor
>from : { <T>(arrayLike: ArrayLike<T>): T[]; <T, U>(arrayLike: ArrayLike<T>, mapfn: (v: T, k: number) => U, thisArg?: any): U[]; <T>(iterable: Iterable<T> | ArrayLike<T>): T[]; <T, U>(iterable: Iterable<T> | ArrayLike<T>, mapfn: (v: T, k: number) => U, thisArg?: any): U[]; }
>{ length: Math.max(e - s, 0) } : { length: number; }
>length : number
>Math.max(e - s, 0) : number
>Math.max : (...values: number[]) => number
>Math : Math
>max : (...values: number[]) => number
>e - s : number
>e : number
>s : number
>0 : 0
>(_, i) => s + i : (_: unknown, i: number) => number
>_ : unknown
>i : number
>s + i : number
>s : number
>i : number
}

// --- Arithmetic ---

const add = (a: number, b: number) => a + b;
>add : (a: number, b: number) => number
>(a: number, b: number) => a + b : (a: number, b: number) => number
>a : number
>b : number
>a + b : number
>a : number
>b : number

const sub = (a: number, b: number) => a - b;
>sub : (a: number, b: number) => number
>(a: number, b: number) => a - b : (a: number, b: number) => number
>a : number
>b : number
>a - b : number
>a : number
>b : number

const mul = (a: number, b: number) => a * b;
>mul : (a: number, b: number) => number
>(a: number, b: number) => a * b : (a: number, b: number) => number
>a : number
>b : number
>a * b : number
>a : number
>b : number

const div = (a: number, b: number) => a / b;
>div : (a: number, b: number) => number
>(a: number, b: number) => a / b : (a: number, b: number) => number
>a : number
>b : number
>a / b : number
>a : number
>b : number

const mod = (a: number, b: number) => a % b;
>mod : (a: number, b: number) => number
>(a: number, b: number) => a % b : (a: number, b: number) => number
>a : number
>b : number
>a % b : number
>a : number
>b : number

const neg = (n: number) => -n;
>neg : (n: number) => number
>(n: number) => -n : (n: number) => number
>n : number
>-n : number
>n : number

type Add<A extends number, B extends number> = Intrinsic<typeof add, [A, B]>;
>Add : intrinsic
>add : (a: number, b: number) => number

type Sub<A extends number, B extends number> = Intrinsic<typeof sub, [A, B]>;
>Sub : intrinsic
>sub : (a: number, b: number) => number

type Mul<A extends number, B extends number> = Intrinsic<typeof mul, [A, B]>;
>Mul : intrinsic
>mul : (a: number, b: number) => number

type R01 = Add<10, 20>;                          // 30
>R01 : 30

type R02 = Sub<100, 42>;                          // 58
>R02 : 58

type R03 = Mul<6, 7>;                             // 42
>R03 : 42

type R04 = Intrinsic<typeof div, [100, 4]>;       // 25
>R04 : 25
>div : (a: number, b: number) => number

type R05 = Intrinsic<typeof mod, [17, 5]>;        // 2
>R05 : 2
>mod : (a: number, b: number) => number

type R06 = Intrinsic<typeof neg, [99]>;           // -99
>R06 : -99
>neg : (n: number) => number

const absVal = (n: number) => Math.abs(n);
>absVal : (n: number) => number
>(n: number) => Math.abs(n) : (n: number) => number
>n : number
>Math.abs(n) : number
>Math.abs : (x: number) => number
>Math : Math
>abs : (x: number) => number
>n : number

type R07 = Intrinsic<typeof absVal, [-50]>;       // 50
>R07 : 50
>absVal : (n: number) => number
>-50 : -50
>50 : 50

// Chaining
type R08 = Mul<Add<10, 20>, 3>;                   // 90
>R08 : 90

type R09 = Sub<Mul<Add<2, 3>, 4>, 1>;             // 19
>R09 : 19

// --- Comparisons ---

const isPositive = (n: number) => n > 0;
>isPositive : (n: number) => boolean
>(n: number) => n > 0 : (n: number) => boolean
>n : number
>n > 0 : boolean
>n : number
>0 : 0

const isEven = (n: number) => n % 2 == 0;
>isEven : (n: number) => boolean
>(n: number) => n % 2 == 0 : (n: number) => boolean
>n : number
>n % 2 == 0 : boolean
>n % 2 : number
>n : number
>2 : 2
>0 : 0

type R10 = Intrinsic<typeof isPositive, [5]>;     // true
>R10 : true
>isPositive : (n: number) => boolean

type R11 = Intrinsic<typeof isPositive, [-3]>;    // false
>R11 : false
>isPositive : (n: number) => boolean
>-3 : -3
>3 : 3

type R12 = Intrinsic<typeof isEven, [4]>;         // true
>R12 : true
>isEven : (n: number) => boolean

type R13 = Intrinsic<typeof isEven, [3]>;         // false
>R13 : false
>isEven : (n: number) => boolean

// --- Strings ---

const toUpper = (s: string) => s.toUpperCase();
>toUpper : (s: string) => string
>(s: string) => s.toUpperCase() : (s: string) => string
>s : string
>s.toUpperCase() : string
>s.toUpperCase : () => string
>s : string
>toUpperCase : () => string

const toLower = (s: string) => s.toLowerCase();
>toLower : (s: string) => string
>(s: string) => s.toLowerCase() : (s: string) => string
>s : string
>s.toLowerCase() : string
>s.toLowerCase : () => string
>s : string
>toLowerCase : () => string

const strLen = (s: string) => s.length;
>strLen : (s: string) => number
>(s: string) => s.length : (s: string) => number
>s : string
>s.length : number
>s : string
>length : number

type R14 = Intrinsic<typeof toUpper, ["hello"]>;  // "HELLO"
>R14 : "HELLO"
>toUpper : (s: string) => string

type R15 = Intrinsic<typeof toLower, ["LOUD"]>;   // "loud"
>R15 : "loud"
>toLower : (s: string) => string

type R16 = Intrinsic<typeof strLen, ["hello world"]>; // 11
>R16 : 11
>strLen : (s: string) => number

// --- Math builtins ---

const clamp = (n: number, lo: number, hi: number) => Math.min(Math.max(n, lo), hi);
>clamp : (n: number, lo: number, hi: number) => number
>(n: number, lo: number, hi: number) => Math.min(Math.max(n, lo), hi) : (n: number, lo: number, hi: number) => number
>n : number
>lo : number
>hi : number
>Math.min(Math.max(n, lo), hi) : number
>Math.min : (...values: number[]) => number
>Math : Math
>min : (...values: number[]) => number
>Math.max(n, lo) : number
>Math.max : (...values: number[]) => number
>Math : Math
>max : (...values: number[]) => number
>n : number
>lo : number
>hi : number

const mySqrt = (n: number) => Math.sqrt(n);
>mySqrt : (n: number) => number
>(n: number) => Math.sqrt(n) : (n: number) => number
>n : number
>Math.sqrt(n) : number
>Math.sqrt : (x: number) => number
>Math : Math
>sqrt : (x: number) => number
>n : number

const myPow = (a: number, b: number) => Math.pow(a, b);
>myPow : (a: number, b: number) => number
>(a: number, b: number) => Math.pow(a, b) : (a: number, b: number) => number
>a : number
>b : number
>Math.pow(a, b) : number
>Math.pow : (x: number, y: number) => number
>Math : Math
>pow : (x: number, y: number) => number
>a : number
>b : number

type R17 = Intrinsic<typeof mySqrt, [16]>;         // 4
>R17 : 4
>mySqrt : (n: number) => number

type R18 = Intrinsic<typeof myPow, [2, 10]>;       // 1024
>R18 : 1024
>myPow : (a: number, b: number) => number

type R19 = Intrinsic<typeof clamp, [-5, 0, 100]>;  // 0
>R19 : 0
>clamp : (n: number, lo: number, hi: number) => number
>-5 : -5
>5 : 5

type R20 = Intrinsic<typeof clamp, [50, 0, 100]>;  // 50
>R20 : 50
>clamp : (n: number, lo: number, hi: number) => number

type R21 = Intrinsic<typeof clamp, [999, 0, 100]>; // 100
>R21 : 100
>clamp : (n: number, lo: number, hi: number) => number

// --- Objects ---

const makeObj = (name: string, age: number) => {
>makeObj : (name: string, age: number) => Record<string, any>
>(name: string, age: number) => {    let r: Record<string, any> = {};    r['name'] = name;    r['age'] = age;    return r;} : (name: string, age: number) => Record<string, any>
>name : string
>age : number

    let r: Record<string, any> = {};
>r : Record<string, any>
>{} : {}

    r['name'] = name;
>r['name'] = name : string
>r['name'] : any
>r : Record<string, any>
>'name' : "name"
>name : string

    r['age'] = age;
>r['age'] = age : number
>r['age'] : any
>r : Record<string, any>
>'age' : "age"
>age : number

    return r;
>r : Record<string, any>

};
type R22 = Intrinsic<typeof makeObj, ["Alice", 30]>;
>R22 : { name: "Alice"; age: 30; }
>makeObj : (name: string, age: number) => Record<string, any>

// { name: "Alice", age: 30 }

// --- Tuples ---

const head = (t: any[]) => t[0];
>head : (t: any[]) => any
>(t: any[]) => t[0] : (t: any[]) => any
>t : any[]
>t[0] : any
>t : any[]
>0 : 0

const tail = (t: any[]) => t.slice(1);
>tail : (t: any[]) => any[]
>(t: any[]) => t.slice(1) : (t: any[]) => any[]
>t : any[]
>t.slice(1) : any[]
>t.slice : (start?: number, end?: number) => any[]
>t : any[]
>slice : (start?: number, end?: number) => any[]
>1 : 1

const myRange = (n: number) => { let arr: number[] = []; let i = 0; while (i < n) { arr = [...arr, i]; i = i + 1; } return arr; };
>myRange : (n: number) => number[]
>(n: number) => { let arr: number[] = []; let i = 0; while (i < n) { arr = [...arr, i]; i = i + 1; } return arr; } : (n: number) => number[]
>n : number
>arr : number[]
>[] : never[]
>i : number
>0 : 0
>i < n : boolean
>i : number
>n : number
>arr = [...arr, i] : number[]
>arr : number[]
>[...arr, i] : number[]
>...arr : number
>arr : number[]
>i : number
>i = i + 1 : number
>i : number
>i + 1 : number
>i : number
>1 : 1
>arr : number[]

type R23 = Intrinsic<typeof head, [[1, 2, 3]]>;    // 1
>R23 : 1
>head : (t: any[]) => any

type R24 = Intrinsic<typeof tail, [[1, 2, 3]]>;    // [2, 3]
>R24 : [2, 3]
>tail : (t: any[]) => any[]

type R25 = Intrinsic<typeof myRange, [5]>;          // [0, 1, 2, 3, 4]
>R25 : [0, 1, 2, 3, 4]
>myRange : (n: number) => number[]

// --- Higher-order: map, filter, reduce ---

const keepPositive = (t: number[]) => t.filter((x: number) => x > 0);
>keepPositive : (t: number[]) => number[]
>(t: number[]) => t.filter((x: number) => x > 0) : (t: number[]) => number[]
>t : number[]
>t.filter((x: number) => x > 0) : number[]
>t.filter : { <S extends number>(predicate: (value: number, index: number, array: number[]) => value is S, thisArg?: any): S[]; (predicate: (value: number, index: number, array: number[]) => unknown, thisArg?: any): number[]; }
>t : number[]
>filter : { <S extends number>(predicate: (value: number, index: number, array: number[]) => value is S, thisArg?: any): S[]; (predicate: (value: number, index: number, array: number[]) => unknown, thisArg?: any): number[]; }
>(x: number) => x > 0 : (x: number) => boolean
>x : number
>x > 0 : boolean
>x : number
>0 : 0

const doubled = (t: number[]) => t.map((x: number) => x * 2);
>doubled : (t: number[]) => number[]
>(t: number[]) => t.map((x: number) => x * 2) : (t: number[]) => number[]
>t : number[]
>t.map((x: number) => x * 2) : number[]
>t.map : <U>(callbackfn: (value: number, index: number, array: number[]) => U, thisArg?: any) => U[]
>t : number[]
>map : <U>(callbackfn: (value: number, index: number, array: number[]) => U, thisArg?: any) => U[]
>(x: number) => x * 2 : (x: number) => number
>x : number
>x * 2 : number
>x : number
>2 : 2

const sum = (t: number[]) => t.reduce((a: number, b: number) => a + b, 0);
>sum : (t: number[]) => number
>(t: number[]) => t.reduce((a: number, b: number) => a + b, 0) : (t: number[]) => number
>t : number[]
>t.reduce((a: number, b: number) => a + b, 0) : number
>t.reduce : { (callbackfn: (previousValue: number, currentValue: number, currentIndex: number, array: number[]) => number): number; (callbackfn: (previousValue: number, currentValue: number, currentIndex: number, array: number[]) => number, initialValue: number): number; <U>(callbackfn: (previousValue: U, currentValue: number, currentIndex: number, array: number[]) => U, initialValue: U): U; }
>t : number[]
>reduce : { (callbackfn: (previousValue: number, currentValue: number, currentIndex: number, array: number[]) => number): number; (callbackfn: (previousValue: number, currentValue: number, currentIndex: number, array: number[]) => number, initialValue: number): number; <U>(callbackfn: (previousValue: U, currentValue: number, currentIndex: number, array: number[]) => U, initialValue: U): U; }
>(a: number, b: number) => a + b : (a: number, b: number) => number
>a : number
>b : number
>a + b : number
>a : number
>b : number
>0 : 0

const unique = (t: any[]) => t.filter((x: any, i: number) => t.indexOf(x) == i);
>unique : (t: any[]) => any[]
>(t: any[]) => t.filter((x: any, i: number) => t.indexOf(x) == i) : (t: any[]) => any[]
>t : any[]
>t.filter((x: any, i: number) => t.indexOf(x) == i) : any[]
>t.filter : { <S extends any>(predicate: (value: any, index: number, array: any[]) => value is S, thisArg?: any): S[]; (predicate: (value: any, index: number, array: any[]) => unknown, thisArg?: any): any[]; }
>t : any[]
>filter : { <S extends any>(predicate: (value: any, index: number, array: any[]) => value is S, thisArg?: any): S[]; (predicate: (value: any, index: number, array: any[]) => unknown, thisArg?: any): any[]; }
>(x: any, i: number) => t.indexOf(x) == i : (x: any, i: number) => boolean
>x : any
>i : number
>t.indexOf(x) == i : boolean
>t.indexOf(x) : number
>t.indexOf : (searchElement: any, fromIndex?: number) => number
>t : any[]
>indexOf : (searchElement: any, fromIndex?: number) => number
>x : any
>i : number

type R26 = Intrinsic<typeof keepPositive, [[1, -2, 3, -4, 5]]>;  // [1, 3, 5]
>R26 : [1, 3, 5]
>keepPositive : (t: number[]) => number[]
>-2 : -2
>2 : 2
>-4 : -4
>4 : 4

type R27 = Intrinsic<typeof doubled, [[1, 2, 3]]>;                // [2, 4, 6]
>R27 : [2, 4, 6]
>doubled : (t: number[]) => number[]

type R28 = Intrinsic<typeof sum, [[1, 2, 3, 4, 5]]>;              // 15
>R28 : 15
>sum : (t: number[]) => number

type R29 = Intrinsic<typeof unique, [[1, 2, 1, 3, 2]]>;           // [1, 2, 3]
>R29 : [1, 2, 3]
>unique : (t: any[]) => any[]

// --- Control flow ---

const factorial = (n: number) => {
>factorial : (n: number) => number
>(n: number) => {    let result: number = 1;    let i: number = n;    while (i > 0) { result = result * i; i = i - 1; }    return result;} : (n: number) => number
>n : number

    let result: number = 1;
>result : number
>1 : 1

    let i: number = n;
>i : number
>n : number

    while (i > 0) { result = result * i; i = i - 1; }
>i > 0 : boolean
>i : number
>0 : 0
>result = result * i : number
>result : number
>result * i : number
>result : number
>i : number
>i = i - 1 : number
>i : number
>i - 1 : number
>i : number
>1 : 1

    return result;
>result : number

};

// for-of loop
const sumLoop = (t: number[]) => {
>sumLoop : (t: number[]) => number
>(t: number[]) => {    let s: number = 0;    for (let x of t) { s = s + x; }    return s;} : (t: number[]) => number
>t : number[]

    let s: number = 0;
>s : number
>0 : 0

    for (let x of t) { s = s + x; }
>x : number
>t : number[]
>s = s + x : number
>s : number
>s + x : number
>s : number
>x : number

    return s;
>s : number

};

// if/else dispatch
const describe = (x: any) => {
>describe : (x: any) => "a boolean" | "a number" | "a string" | "something else"
>(x: any) => {    if (typeof x == 'number') return 'a number';    if (typeof x == 'string') return 'a string';    if (typeof x == 'boolean') return 'a boolean';    return 'something else';} : (x: any) => "a boolean" | "a number" | "a string" | "something else"
>x : any

    if (typeof x == 'number') return 'a number';
>typeof x == 'number' : boolean
>typeof x : "bigint" | "boolean" | "function" | "number" | "object" | "string" | "symbol" | "undefined"
>x : any
>'number' : "number"
>'a number' : "a number"

    if (typeof x == 'string') return 'a string';
>typeof x == 'string' : boolean
>typeof x : "bigint" | "boolean" | "function" | "number" | "object" | "string" | "symbol" | "undefined"
>x : any
>'string' : "string"
>'a string' : "a string"

    if (typeof x == 'boolean') return 'a boolean';
>typeof x == 'boolean' : boolean
>typeof x : "bigint" | "boolean" | "function" | "number" | "object" | "string" | "symbol" | "undefined"
>x : any
>'boolean' : "boolean"
>'a boolean' : "a boolean"

    return 'something else';
>'something else' : "something else"

};

type R30 = Intrinsic<typeof factorial, [5]>;       // 120
>R30 : 120
>factorial : (n: number) => number

type R31 = Intrinsic<typeof sumLoop, [[1, 2, 3, 4, 5]]>; // 15
>R31 : 15
>sumLoop : (t: number[]) => number

type R32 = Intrinsic<typeof describe, [42]>;       // "a number"
>R32 : "a number"
>describe : (x: any) => "a boolean" | "a number" | "a string" | "something else"

type R33 = Intrinsic<typeof describe, ["hi"]>;     // "a string"
>R33 : "a string"
>describe : (x: any) => "a boolean" | "a number" | "a string" | "something else"

type R34 = Intrinsic<typeof describe, [true]>;     // "a boolean"
>R34 : "a boolean"
>describe : (x: any) => "a boolean" | "a number" | "a string" | "something else"
>true : true

// --- String transforms ---

const camelCase = (s: string) => {
>camelCase : (s: string) => string
>(s: string) => {    let [first, ...rest] = s.split('_');    return first + rest.map((p: string) => capitalize(p)).join('');} : (s: string) => string
>s : string

    let [first, ...rest] = s.split('_');
>first : string
>rest : string[]
>s.split('_') : string[]
>s.split : { (separator: string | RegExp, limit?: number): string[]; (splitter: { [Symbol.split](string: string, limit?: number): string[]; }, limit?: number): string[]; }
>s : string
>split : { (separator: string | RegExp, limit?: number): string[]; (splitter: { [Symbol.split](string: string, limit?: number): string[]; }, limit?: number): string[]; }
>'_' : "_"

    return first + rest.map((p: string) => capitalize(p)).join('');
>first + rest.map((p: string) => capitalize(p)).join('') : string
>first : string
>rest.map((p: string) => capitalize(p)).join('') : string
>rest.map((p: string) => capitalize(p)).join : (separator?: string) => string
>rest.map((p: string) => capitalize(p)) : string[]
>rest.map : <U>(callbackfn: (value: string, index: number, array: string[]) => U, thisArg?: any) => U[]
>rest : string[]
>map : <U>(callbackfn: (value: string, index: number, array: string[]) => U, thisArg?: any) => U[]
>(p: string) => capitalize(p) : (p: string) => string
>p : string
>capitalize(p) : string
>capitalize : (s: string) => string
>p : string
>join : (separator?: string) => string
>'' : ""

};

// Deep property access: reduce over a split path
const deepGet = (t: any, path: string) =>
>deepGet : (t: any, path: string) => any
>(t: any, path: string) =>    path.split('.').reduce((obj: any, key: string) => obj[key], t) : (t: any, path: string) => any
>t : any
>path : string

    path.split('.').reduce((obj: any, key: string) => obj[key], t);
>path.split('.').reduce((obj: any, key: string) => obj[key], t) : any
>path.split('.').reduce : { (callbackfn: (previousValue: string, currentValue: string, currentIndex: number, array: string[]) => string): string; (callbackfn: (previousValue: string, currentValue: string, currentIndex: number, array: string[]) => string, initialValue: string): string; <U>(callbackfn: (previousValue: U, currentValue: string, currentIndex: number, array: string[]) => U, initialValue: U): U; }
>path.split('.') : string[]
>path.split : { (separator: string | RegExp, limit?: number): string[]; (splitter: { [Symbol.split](string: string, limit?: number): string[]; }, limit?: number): string[]; }
>path : string
>split : { (separator: string | RegExp, limit?: number): string[]; (splitter: { [Symbol.split](string: string, limit?: number): string[]; }, limit?: number): string[]; }
>'.' : "."
>reduce : { (callbackfn: (previousValue: string, currentValue: string, currentIndex: number, array: string[]) => string): string; (callbackfn: (previousValue: string, currentValue: string, currentIndex: number, array: string[]) => string, initialValue: string): string; <U>(callbackfn: (previousValue: U, currentValue: string, currentIndex: number, array: string[]) => U, initialValue: U): U; }
>(obj: any, key: string) => obj[key] : (obj: any, key: string) => any
>obj : any
>key : string
>obj[key] : any
>obj : any
>key : string
>t : any

type R35 = Intrinsic<typeof camelCase, ["user_first_name"]>;       // "userFirstName"
>R35 : "userFirstName"
>camelCase : (s: string) => string

type R36 = Intrinsic<typeof camelCase, ["get_all_items_by_id"]>;   // "getAllItemsById"
>R36 : "getAllItemsById"
>camelCase : (s: string) => string

type R37 = Intrinsic<typeof deepGet, [{ a: { b: { c: 42 } } }, "a.b.c"]>; // 42
>R37 : 42
>deepGet : (t: any, path: string) => any
>a : { b: { c: 42; }; }
>b : { c: 42; }
>c : 42

// --- Generics: deferred evaluation ---

type GenericAdd<A extends number, B extends number> = Add<A, B>;
>GenericAdd : intrinsic

function testGeneric<X extends number>(x: X): GenericAdd<X, 1> {
>testGeneric : <X extends number>(x: X) => GenericAdd<X, 1>
>x : X

    return undefined!;
>undefined! : never
>undefined : undefined
}

